/** Locale namespace for this plugin's dictionaries. */
export const NS = 'run-env-manager';
/** 必须与 cordis.patch.yml 的行 id、宿主 SETTINGS_NAMESPACE 三处一致。 */
export const SETTINGS_NAMESPACE = 'run-env-manager';
/** Tab-type implementation id; the body/title seats register under this key. */
export const TAB_ID = 'run-env-manager';
/** Kind the guide capsule opens and `openTab` names. */
export const TAB_KIND = 'run-env';
/** Host route prefix. */
export const ROUTE = '/dsh-run-env';
/** 阶段 0 每个工作区只跑一个实例。 */
export const RUN_ID = 'spike';
